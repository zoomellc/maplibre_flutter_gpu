/// Well-known MapLibre style URLs, mostly for demos and docs.
class MapLibreStyles {
  MapLibreStyles._();

  /// MapLibre demo tiles (vector, global, free).
  static const String demo = 'https://demotiles.maplibre.org/style.json';

  /// OpenFreeMap Liberty — full basemap suitable for real apps.
  static const String openFreeMapLiberty =
      'https://tiles.openfreemap.org/styles/liberty';
}
