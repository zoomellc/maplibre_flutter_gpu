name: visual_e2e_gpu
description: Android visual E2E fixture for maplibre_flutter_gpu.
publish_to: none
version: 0.1.0+1

environment:
  sdk: ^3.11.1
  flutter: '>=3.38.0'

dependencies:
  flutter:
    sdk: flutter
  maplibre_flutter_gpu:
    path: ../../..
  visual_e2e_shared:
    path: ../shared

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: false
