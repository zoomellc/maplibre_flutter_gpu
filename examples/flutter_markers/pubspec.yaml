name: maplibre_flutter_gpu_flutter_markers_example
description: Flutter marker overlay example for maplibre_flutter_gpu.
publish_to: 'none'
version: 0.0.1-dev.0

environment:
  sdk: ^3.11.1
  flutter: '>=3.38.0'

dependencies:
  flutter:
    sdk: flutter
  maplibre_flutter_gpu:
    path: ../../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
